# Svmemo

给中文母语者用的瑞典语背单词 App：间隔重复（ts-fsrs，接近墨墨）+ 拼写。

数据只存在**当前浏览器**的 IndexedDB，不上传服务器，也没有账号。Cursor 预览和 Chrome 各存一份、互不同步；换浏览器用「复制进度 / 粘贴导入」，或「导出备份 / 导入备份」。

界面是中文，`lang="zh-CN"`。浅色 / 深色跟随系统 `prefers-color-scheme`。

## 用户是谁

- 中文母语，正在学瑞典语（日常生活 / SFI）
- 每天学十几分钟
- 词条要有可靠的中文释义、变形和例句，不编造词典里没有的义项

## 现在做成了什么

1. 词库是一份书，进度是一份：选词进计划，不默认全背
2. 一词一卡（FSRS 记在这一张上）；学习时再选认识或拼写
3. 今日：先到期拼写，再按每 7 个新词「先看卡片、再立刻拼写」
4. 认识卡只预习、不打分；间隔只由拼写对错写入 ts-fsrs
5. 收藏（单词本）和已掌握（不再进每日队列和普通复习）
6. 今日页上的学习日历；备份导出 / 导入 / 剪贴板复制粘贴
7. 三种开练入口：今日待学、复习单词本、例句填空。点「到期复习」数字可看已学词还要几天
8. 认识卡 / 拼写卡进入时自动朗读该词；例句填空不自动读
9. 学习卡片上可收藏、标已掌握（Ctrl+S / Ctrl+M）；到期列表每行也可收藏 / 掌握

## 明确不做

多设备云同步、账号、社交、AI 讲词、语法课、付费、把 SVALex 1.5 万词一次性灌进来。不做 PWA / 离线包。

设置表里仍存着 `dailyLimit: 40` / `maxNewPerDay: 15` / `spellingRatio: 0.5` 的默认值，但**没有设置页**，每日队列也**不再按这两个上限截断**。

## 页面

三个 Tab（底栏）。统计、日历和备份都放在「今日」，不单独做设置页。

| Tab | 作用 |
| --- | --- |
| 今日 | 日期、今日待学（到期复习可点进去看列表 + 新词）、开始背单词、复习单词本、练例句、学习日历、去词库 / 例句、备份 |
| 例句 | 看中文，把要学的词写进句子；可练计划中的例句或单词本例句 |
| 词库 | 搜索、筛选（全部 / 未加入 / 计划中 / 单词本 / 已掌握）、勾选批量加入计划、单行加入/移出、收藏、标已掌握 |

学习中途全屏进入会话（底栏关掉），点「结束」或全部做完后回到今日。已经写下的进度会留下；中途退出**不会**把当天标成打卡完成。

### 今日

- 主卡片：**今日待学** = 到期复习数 + 新词数，旁边一个主按钮。计划里还没有词时是「去词库选词」；一旦有词，按钮一直是「开始背单词」（今天没有到期任务时点进去会提示，不会把按钮藏掉）。到期任务背完应收成 0，之后再按 FSRS 到期出现。
- 底下拆开显示到期 / 新词，并写一句今天怎么学。点「到期复习」数字进入列表：所有已学、未掌握的词，不设上限。上面是**这一刻到期**的（按遗忘曲线低的在前），下面是还没到点的，并写明天 / N 天后。今天刚背过的词会排到后面几天，不是丢了。列表每行可收藏 / 标已掌握；点「开始拼写」只练到期的那些，对错写入 ts-fsrs。中途结束，已经写下的进度会留下。
- 摘要条：今天已学 · 计划中 · 已掌握（有才显示）· 词库剩。
- 加练两张小卡片：单词本、例句。卡片上**不放大显示总数**。
- 「复习单词本」：所有收藏词，见下方「单词本」。
- 「开始填空」：计划中未掌握词的全部例句。也可进「例句」页改练单词本。
- 计划中为 0 时，提示进度只在当前浏览器，Cursor 预览和 Chrome 不同步。

### 词库

- 标题「我的词表」。搜索框匹配 lemma（`sv-SE` 小写）、中文释义，以及英文释义（如有）。
- 筛选：全部 / 未加入 / 计划中 / 单词本 / 已掌握，标签上显示该类词数。「未加入」= 不在计划且未掌握。
- 列表按：先计划中未掌握，再词表原始顺序。筛「单词本」时收藏仍排在前面。
- 行上显示：lemma、性 · 词性、收藏 / 已掌握标记、中文、构词摘要、变形摘要。不展示例句。
- 未加入且未掌握：点行勾选，再用工具栏「把 N 个词加入计划」；行上也可点「加入」。工具栏有「全选未加入」「清除选择」（范围是当前筛选结果里还能加入的词）。
- 已在计划且未掌握：点行或点「移出」会移出计划。其中有过学习记录（`reps > 0`）时先确认：移出后复习进度会清掉。
- 已掌握：不能勾选、不能加入/移出；只能取消掌握或改收藏。
- 星标随时可点；掌握随时可点。

### 例句

看中文，句子里前后的瑞典语已经写好，只默写本句要练的那个词（及其在句中的变形）。空格、标点由题目补上，只打字母；å ä ö 按钮仍在。必须全对才能下一句；Hard / Again 当场重打。

- 句子来自词条自带的 3–4 条例句。计划中未掌握的词，或单词本（含已掌握收藏）。
- 提示只给要填的词：词性、中文意思、以及不是词典形时的变形（现在时 / 定式单数等）。名词带 en/ett。**不给这个词的瑞典语拼写**。
- 找不到该词在句中的位置时，才退回成默写整句。
- 可听整句。揭晓后给出对照、原句（要填的词会标出），以及该词的构词 / 变形 / 例句。
- 打乱后仍按 7 句一组；组间有过渡。中途可结束。
- **不写 FSRS**，也不把当天标成打卡完成。卡片上仍可收藏或标已掌握；标掌握后当前句立刻跳过，本场后面同一词的句子也会撤掉。

## 词库

- 当前词表在 `src/data/custom-words.ts`，`src/data/a1.ts` 只是再导出。启动时 `seedIfEmpty()` **整表同步**词条（覆盖释义、变形、例句、构词），学习进度留在 `cards` / `reviewLogs` / `wordMarks`
- 现在约 **641** 个自选词，词条 CEFR 一律标成 B1（构建函数里先写 A1，再由 `b1()` 改掉）。词表这块「我的词表」的 CEFR 也是 B1
- 大约：名词 322、动词 140、形容词 91、副词 51、短语 24、代词 4、其他 7、感叹 2。名词 / 动词 / 形容词带变形，每词 3–4 条例句；不少词带 `compound` 构词
- 词条从词表里消失时，对应卡片、复习日志和标记一并清掉（换 lemma 等于新词）
- 主键是 `id`（`{lemma}__{pos}`，空格改成下划线），不是 lemma 本身（同一个写法可以是不同词性）
- 能拆的词写 `compound`：各部分的瑞典语 + 中文，以及合起来为什么是这个意思。认识卡翻面、拼写揭晓、词库列表都会显示
- 单纯词、外来词、只是动词加 `-ande` 的分词不硬拆
- 类型里还有 `intj` / `proper`；词表里 `hurra` 用了感叹词

### 加新词的校对

1. 先查 [Folkets lexikon](https://folkets-lexikon.csc.kth.se/folkets/)，必要时再对 SAOL / Wiktionary
2. 中文释义只写词典里有的义项；多义词标出来，不要把名词义和动词义写进同一张动词卡
3. 变形跟词典走；没有的定式 / 复数不要编
4. 复合词或明显前缀/后缀，补上构词

## 学习计划

- 词库里「加入计划」才会生成卡片。未加入的词只出现在词库，不进今日新词
- **已掌握**的词不进每日学习和普通复习；可在词库或卡片上取消。标掌握**不会**删卡片
- **收藏**进单词本，和是否在计划里无关
- 从计划移出：删掉该词的卡片和复习日志，标记还在
- 加入计划时，若该词当时是已掌握，会把掌握清掉（词库界面本身不让对已掌握词点「加入」；单词本开练时可能走到这条路径）

### 单词本

今日「复习单词本」会：

1. 把还没卡片的收藏词先加入计划（没有卡片的已掌握收藏词，加入时会取消已掌握）
2. 把**所有收藏词**（含已掌握）打乱后只做拼写，仍按 7 个一组
3. 新收藏、`reps === 0` 的词也直接拼写，没有认识卡预习

## 每日队列

```
到期复习 = 计划中、reps > 0、未掌握，并且：
  - 复习态：due 落在今天结束前（本地自然日，接近墨墨；今天下午才到点的词早上也算）
  - 学习中 / 重学中（1m / 10m）：due ≤ 现在
今日新词 = 计划中、reps === 0、未掌握（有多少背多少，不再截成 15 个）
今日队列 = 到期拼写 + 每 7 个新词（先认识卡预习，再立刻拼写）
```

今日页会在重新打开、回到前台、以及每 30 秒按**当前时刻**重算待学数，不会把早上打开时的数字冻住。跨过午夜也会换成新的一天。

另几场，都不走上面的「先认识再拼写」：

| 入口 | 范围 | 题型 |
| --- | --- | --- |
| 开始背单词 | 到期 + 今日新词 | 到期只拼写；新词每 7 个先预习再拼写 |
| 到期复习（点数字） | 已学未掌握全部列出；到期的在前，其余标明还要几天 | 先看列表；开始拼写只练到期的，对错写 FSRS |
| 复习单词本 | 全部收藏（见上） | 打乱后只拼写，7 个一组 |
| 练例句 | 计划中或单词本的例句 | 看中文把要学的词写进句子，7 句一组，不写 FSRS |

- 认识卡预习（`kind: learn`）只翻面看释义，**不写 FSRS**
- 同一组随后的拼写，以及到期复习 / 单词本加练的拼写，才 `submitReview`
- 到期复习按 due 从早到晚；新词按加入计划的卡片 id
- 组与组之间全屏停一句：到期复习过完、或某一组新词/加练过完。回车继续
- 会话可中途结束；进度已经写下的会留下
- 整场做完才把当天 `dailyStats.completed` 打上（加练场做完也会打）

## 题型

回车或 →：认识卡未翻面 → 翻面；已翻面的预习卡 → 下一张；拼写/例句揭晓后 → 下一题或再试；组间/结束页 → 继续。背单词时 ← 回到上一张（已写下的进度不改；回到已拼过的词再答一遍不会再写 FSRS）。

认识卡、拼写卡、例句卡右上角可收藏或标已掌握。学习中 **Ctrl+S** 把当前词加入单词本（已收藏则保持）；**Ctrl+M** 标为已掌握并立刻跳到下一张。标已掌握后本场后面同一词的题目也会撤掉。取消掌握不会跳走。到期复习列表每行也可收藏 / 掌握；标掌握后该行立刻从列表消失。

### 认识卡

只在学新词时出现，用来见面，不打分、不写 FSRS。

正面：瑞典语 + 朗读。刚进入这张卡时自动读一次该词；按钮仍可再听。点卡片或「显示释义（回车）」翻面。翻面：中文、英文释义（如有）、性、构词、变形表、例句。笔记本宽度下左右两栏（左栏词义 / 构词 / 变形，右栏例句），一屏看完，不用往下滚。窄屏仍上下排，必要时卡片内滚动。按钮只有「下一张」。

代码里还留着忘记 / 困难 / 认识 / 简单四键，但当前队列里认识卡全是预习，这四个键不会出现。

### 拼写卡

看中文（可听发音，英文释义若有也会显示）把瑞典语打出来。不要做「看瑞典语敲中文」。刚进入这张卡时自动读一次该词；「听发音」仍可再听。

- 输入区按字母位数显示空位；空格、连字符等非字母由题目自己补上（短语如 `i synnerhet` 只打字母）
- 只收 `a–zåäö`；输入时 `sv-SE` 小写，超长截掉
- 手机 / 桌面都提供 å ä ö 按钮
- 字母空位填满会自动检查；也可点「检查」（空着不能提交）
- 比对前：Unicode NFC、trim、`sv-SE` 小写
- 拼错出示答案（错位用红字标**你打出来的字符**），并给出变形 / 构词 / 例句
- 第一次作答写入 FSRS；再试只练，不再改间隔
- **必须全对才能进下一题**。Hard（只错 å/ä/ö，或编辑距离 1）也要当场重打，不另排到队尾

| 结果 | Rating | 界面 |
| --- | --- | --- |
| 全对 | Good | 正确 → 下一题 |
| 只错 å/ä/ö，或编辑距离 1 | Hard | 很接近 → 再试一次 |
| 其他错误 | Again | 再练一次 → 再试一次 |

`Easy` 不会由拼写打出。

## 朗读

1. 先试 Web Speech，语言 `sv-SE`（没有列出瑞典语语音时也先试；语速 0.9）
2. 不行再走 Google Translate TTS 音频（需要联网）
3. 都失败才提示换 Chrome 或联网，不假装读过

认识卡按钮是「发音」，拼写卡是「听发音」。刚进入认识卡或拼写卡时各自动读一次该词 lemma；翻面不重读，切走当前卡会停掉。例句填空不自动读（会泄题；仍可点「听整句」）。

## 学习日历

今日页上的月历，周一为一周第一天。可翻月，不在本月时出现「今天」。

- 格子：有记录则显示 `新学 / 复习` 两个数字
- 月头：本月新学 · 复习 · 打卡天数（有新学或复习的天数）
- 点某一天：新学 / 复习各几个、作答几次、是否「已完成当日任务」，以及当天词列表（lemma + 中文）

日历里的「新学 / 复习」按**该词第一次写入复习日志的本地日期**划分，和当日 `dailyStats` 的计数可能略有出入。没有日志、只剩旧统计行时，才回退到那行的数字。旧版配对留下的 `matchedWordIds` 仍会并入当日「复习」。

## 瑞典语词条规则

- 名词：`gender: "en" | "ett"`（个别可空），以及有的定式单数、不定复数、定式复数
- 动词：原形、现在时、过去时、完成时（supinum）、命令式（有的才写）
- 形容词：en 形、ett 形（加 t）、复数/定式（加 a）；比较级 / 最高级、单独定式有的才写
- 代词若带 ett 形 / 复数，变形表按形容词那套显示
- 副词、代词、短语、介词等用 `other()`，pos 写成 `adv` / `pron` / `phrase` / `other`
- 每词 3–4 条例句，尽量覆盖不同变形
- 拼写比较必须先 NFC

## 数据

| 表 | 作用 |
| --- | --- |
| `words` | 启动时从静态词库覆盖写入 |
| `decks` / `deckWords` | 目前一个词表「我的词表」（id 仍是 `a1-core`） |
| `cards` | 一词一卡，FSRS 字段；`cardType` 固定写成 `recognition`，真正题型在队列里选 |
| `reviewLogs` | 每次计分的作答 |
| `dailyStats` | 按本地日历日汇总新学 / 复习次数，以及当天是否打卡完成；`matchedWordIds` 是旧版配对留下的字段，日历仍会读 |
| `wordMarks` | 已掌握、收藏；两项都关则删行 |
| `settings` | 默认行：未使用的每日上限字段，以及旧版 `recentMatchWordIds`（缺省当 `[]`） |

- 进度只写 Dexie，不另做 Zustand/Redux
- 改表结构必须升 `db.version` 并写 `upgrade`（当前 v4：去掉已废弃的 `readings` / `readingNotes`；v2 是 `wordMarks`）。`settings.recentMatchWordIds` 和 `dailyStats.matchedWordIds` 是对象上的旧字段，不升版本；缺省当 `[]`。
- 备份 JSON（`BACKUP_VERSION = 2`，仍能导入 v1 / v3）含词库快照和进度；导入前确认，然后**整库覆盖**当前浏览器。导出文件名 `svmemo-backup-YYYY-MM-DD.json`。下次打开仍会用静态词表覆盖 `words`，卡片 / 日志 / 标记按现有词 id 保留
- 算法是纯函数（队列、拼写比对、FSRS 封装）；组件 / Hook 只负责读写和翻下一张

FSRS：`request_retention: 0.9`，`enable_fuzz`，`enable_short_term`，学习步 `1m` / `10m`，重学步 `10m`。

## 技术

React 19 + TypeScript + Vite。Dexie + `dexie-react-hooks`。`ts-fsrs`。Zod 校验词条。Lint 用 oxlint。

## 接下来（还没做）

- 继续往词表加词（先校对再入库），目标仍是几百词这个量级
- 设置页：若要恢复每日上限，再接到 `planDailyQueue`
- SVALex 只作以后选词参考，不当词书；它没有释义和例句，许可证 CC BY-NC-SA 4.0，用到时页面要署名
