# Svmemo

给中文母语者用的瑞典语背单词 App：间隔重复（接近墨墨）+ 拼写。

数据存在浏览器 IndexedDB，不上传服务器。第一期只做单设备。

## 用户是谁

- 中文母语，瑞典语大约 A1，目标是能应付日常生活和 SFI 课堂
- 每天学 10–20 分钟
- 第一批只背 A1 核心词（名词/动词/形容词带完整变形和例句）

## 抄墨墨的 6 件事

1. 词库是书，进度是一份：同一个词在多个词库里都算已学
2. 选词进计划，不要默认全背
3. 每日配额：先复习到期词，再用剩余名额学新词
4. 两种题型：认识卡 + 拼写
5. 每次作答更新 FSRS 间隔
6. 连续打卡；统计先做数字，不做复杂图表

## 明确不做（第一期）

多设备云同步、账号、社交、AI 讲词、语法课、付费、把 SVALex 1.5 万词一次性灌进来。

## 页面

| Tab | 作用 |
| --- | --- |
| 今日 | 新词 X + 复习 Y，开始学习 |
| 词库 | 按 CEFR / 主题浏览、搜索、批量加入 |
| 统计 | 打卡、已学、待复习 |
| 设置 | 每日上限、新词上限、题型比例、导出/导入备份 |

第一期垂直切片只需要「今日学习」能走通。词库 / 统计 / 设置可以后补，但备份导出要尽早做。

## 每日抽题（Quizlet 式分组）

先复习到期词。新词每 **7 个**一组：

1. 先翻卡片熟悉这 7 个（不记间隔）
2. 立刻拼写复习这 7 个（这时才写入 FSRS）
3. 过完一组再加下一组新词

```
due        = 今天到期的卡片
newCount   = min(dailyLimit - due, maxNewPerDay, 未学数量)
今日队列    = 到期拼写 + 每 7 个新词（先学习后复习）
```

默认：`dailyLimit = 40`，`maxNewPerDay = 15`（大约两组新词）。复习已经 40 个时，当天新词为 0。

计划中的词即使还没到期，用户也可以在今日页点「复习计划中的词」随时开拼写复习。

## 题型

### 认识卡

正面：瑞典语 + 发音。翻面：中文、变形表、3–4 条例句。四键对应 ts-fsrs：

| 按钮 | Rating |
| --- | --- |
| 忘记 | Again |
| 困难 | Hard |
| 认识 | Good |
| 简单 | Easy |

不要自己发明三档算法。

### 拼写卡（复习强制）

新词先用认识卡见面。到期复习必须看中文（或听发音）把瑞典语打出来。输入区按字母位数显示短杠，拼对就进入下一个词；拼错会出示答案并当场再试，同时把下次复习间隔缩短。不要做「看瑞典语敲中文」。

比对前：Unicode NFC、trim、小写。

| 结果 | Rating |
| --- | --- |
| 全对 | Good（很快可记 Easy） |
| 编辑距离 1，或只错 å/ä/ö | Hard，立刻再练 |
| 其他错误 | Again，立刻再练 |

输入框在手机上要提供 å ä ö 按钮。

## 瑞典语规则

- 名词存 `gender: "en" | "ett"`，以及定式单数、不定复数、定式复数
- 动词存原形、现在时、过去时、完成时（supinum）、命令式
- 形容词存 en 形、ett 形（加 t）、复数/定式（加 a）；不规则的另存定式单数（如 liten → lilla / små）
- 每词 3–4 条例句，尽量覆盖不同变形
- 主键是 `id`（如 `bil__noun`），不是 lemma 字符串（`resa` 可以是动词也可以是名词）
- 拼写比较必须先 NFC

## 数据原则

- 进度只写 Dexie，不另做 Zustand/Redux
- 静态词库随 App 发布；学习进度留在用户浏览器
- 改表结构必须升 `db.version` 并写 `upgrade`
- IndexedDB 可能被浏览器清掉，所以要有 JSON 导出/导入
- SVALex 只用来以后分级，不把它当成词书。它没有释义和例句，许可证是 CC BY-NC-SA 4.0，页面以后要署名

## 内容节奏

1. 现在：校对过的 A1 词（`src/data/a1.ts`），动词/名词/形容词带完整变形和 3–4 条例句
2. 闭环能用之后：继续扩到 300–500
3. 再用 SVALex 做 A1/A2 候选清单，释义另补

## 技术

React 19 + TypeScript + Vite。Dexie + `dexie-react-hooks`。`ts-fsrs`。Zod 校验词条和备份。语音用 Web Speech API（`sv-SE`）；没有瑞典语语音时要提示，不能假装能发音。

算法做成纯函数（调度、拼写比对），Hook 只负责读写数据库和翻下一张卡。

## 完成标准（垂直切片）

用当前 A1 词连续学习 3 天，第二天打开进度还在，并且能导出备份。
